# 2026-03-14

- Continued the `lockscreentodo` Supabase 500 investigation.
- Replaced module-scope Supabase client initialization in high-risk Next.js API routes with a shared server helper created inside request handlers.
- Rewrote corrupted `generate/check-limit` and `user/notion-status` routes to restore valid request handling and clearer configuration errors.
- Deleted stale `BOOTSTRAP.md` because workspace initialization had already been completed.
- Verification status: targeted ESLint is blocked by an existing repo config issue; full TypeScript check is blocked by an unrelated syntax error in `apps/nextjs/src/app/[lang]/(dashboard)/echocv/resume-editor/components/ProfessionalResumeEditor.tsx`.
