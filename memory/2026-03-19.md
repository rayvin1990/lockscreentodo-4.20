# 2026-03-19

## Session Snapshot
- Identity name confirmed as 奥特 and rewritten into startup memory files.
- Recovered the repo-local ACPX/OpenClaw bridge enough for one-shot `openclaw exec` to work again.
- Remaining limitation: ACPX named session management for `openclaw` still fails with `spawn EPERM`.

## ACPX Local Recovery (13:40:00)
- Investigated report that `acpx` was unusable again.
- Root cause:
  - repo-local ACPX wrapper files had disappeared: `.acpxrc.json`, `acpx.cmd`, `acpx.ps1`
  - `scripts/openclaw-acp-runner.mjs` was also missing
  - fallback global `C:\Users\57684\.acpx\config.json` still launched `openclaw` through `cmd /c ...`, which reproduced `spawn EPERM`
- Fixes applied in workspace:
  - restored `.acpxrc.json` so project-local `openclaw` uses `node C:/Users/57684/saasfly/scripts/openclaw-acp-runner.mjs`
  - restored `acpx.cmd` / `acpx.ps1` to run `vendor/acpx/dist/cli.js` with `ACPX_STATE_DIR=C:\Users\57684\saasfly\tmp\acpx-state`
  - restored `vendor/acpx/package.json` with `"type": "module"`
  - rebuilt `scripts/openclaw-acp-runner.mjs` so it:
    - derives ACP-only state under `tmp/openclaw-acp-state`
    - writes `gateway.token`
    - writes a reduced `openclaw.json` with `bindings: []`, `channels: {}`, and `plugins.allow = ["acpx"]`
    - launches `openclaw acp` with `OPENCLAW_CONFIG_PATH`, `OPENCLAW_STATE_DIR`, `OPENCLAW_SKIP_CHANNELS=1`, and `--token-file`
- Verification:
  - `cmd /c .\acpx.cmd config show` loads the project config and local state path
  - `cmd /c .\acpx.cmd --verbose openclaw exec "Reply with OK only."` now succeeds and prints `OK`
- Remaining issue:
  - inside the current Codex sandbox, `sessions new/ensure` can still throw `spawn EPERM`
  - this turned out to be a sandbox artifact, not the real machine state

## ACPX External Verification (15:58:00)
- Re-ran the repaired repo-local ACPX/OpenClaw flow outside the sandbox.
- Verified successful commands:
  - `cmd /c .\acpx.cmd --verbose openclaw sessions new --name acpx-local-fixed`
  - `cmd /c .\acpx.cmd --verbose --format json openclaw sessions ensure --name acpx-local-fixed`
  - `cmd /c .\acpx.cmd openclaw sessions list`
  - `cmd /c .\acpx.cmd openclaw status -s acpx-local-fixed`
  - `cmd /c .\acpx.cmd --verbose openclaw exec "Reply with OK only."`
- Verified status:
  - named session `acpx-local-fixed` is running with a live owner PID
  - one-shot `openclaw exec` returns `OK`
- Additional hardening:
  - `.acpxrc.json` now points `openclaw` at an absolute `C:/Program Files/nodejs/node.exe`
  - local `vendor/acpx/dist/session-BARXiu6-.js` now retries Windows `EPERM` spawns via `shell: true`
- Final conclusion:
  - repo-local `.\acpx.cmd` is working on the real machine
  - remaining `spawn EPERM` results seen inside this Codex session are sandbox-only and should not be treated as a live regression

## ACPX Global Alignment (16:08:00)
- Updated `C:\Users\57684\.acpx\config.json` so global `acpx` points `openclaw` at:
  - `"C:/Program Files/nodejs/node.exe" C:/Users/57684/saasfly/scripts/openclaw-acp-runner.mjs`
- Saved backup:
  - `C:\Users\57684\.acpx\config.json.bak.20260319-2`
- Verified outside the sandbox that global `acpx --verbose openclaw exec "Reply with OK only."` now succeeds and returns `OK`.

## Dropout Investigation (17:00:00)
- Investigated report that `openclaw gateway`, Claude Code, and Codex terminals were all dropping offline.
- Confirmed three separate factors:
  - `C:\Users\57684\.openclaw\openclaw.json` currently starts with a UTF-8 BOM (`EF BB BF`), which made `scripts/openclaw-acp-runner.mjs` fail on `JSON.parse`
  - patched `scripts/openclaw-acp-runner.mjs` to strip a leading BOM before parsing
  - after the patch, repo-local `.\acpx.cmd --verbose openclaw exec "Reply with OK only."` succeeds again and returns `OK`
- Additional state findings:
  - named ACPX session `acpx-local-fixed` still points at an old dead record and `status -s acpx-local-fixed` reports `dead`
  - `sessions ensure --name acpx-local-fixed` reuses that stale record instead of replacing it
  - creating a new named session from inside this Codex environment still throws `spawn EPERM`, consistent with earlier sandbox-only behavior
- Guardian interference evidence:
  - `C:\Users\57684\AppData\Roaming\openclaw-guardian\logs\guardian-2026-03-16.log` shows repeated cycles of:
    - `OpenClaw may be hung (no output for 15.0 minutes), restarting...`
    - `OpenClaw exited with code 1`
    - `Scheduling restart ...`
    - `OpenClaw started with PID ...`
  - restart attempts reached at least `attempt 44`, so Guardian has a real history of force-restarting OpenClaw based on inactivity/no-output heuristics

## Saasfly EdgeOne Deployment - Neon Database Migration (17:25-17:38)
- User asked to deploy saasfly to Tencent Cloud EdgeOne (腾讯云 EdgeOne)
- Original issue: Supabase doesn't work in China, must use Neon PostgreSQL instead
- Worked on converting code from Supabase to Neon:
  - Installed `@neondatabase/serverless` package
  - Created `apps/nextjs/src/lib/neon/server.ts` Neon client wrapper
  - Updated `env.mjs` to support NEXT_PUBLIC_NEON_DATABASE_URL
  - Updated tsconfig.json to add `@/*` path alias
  - Converted 8+ API routes to use Neon:
    - `api/download/record-usage/route.ts`
    - `api/download/check-limit/route.ts`
    - `api/generate/record-usage/route.ts`
    - `api/generate/check-limit/route.ts`
    - `api/debug/supabase/route.ts`
    - `api/user/usage-stats/route.ts`
    - `api/user/wallpaper-history/route.ts`
    - `api/wallpaper/save/route.ts`
    - `api/notion/tasks/route.ts`
- Added environment variables to EdgeOne project settings:
  - NEXT_PUBLIC_NEON_DATABASE_URL (user provided connection string)
  - NEXT_PUBLIC_APP_URL (added as https://saasfly.edgeone.app)
- Deployment still failing: NEXT_PUBLIC_APP_URL missing caused build error
- Frontend files still need conversion (generator/page.tsx, lib/supabase/realtime.ts)
- Gateway has been unstable during this session, multiple timeouts

## Identity Update (17:35)
- Confirmed identity: I am 小卡 (Claude carrier), not 奥特 (Codex carrier)
- User clarified: 奥特 is the agent in Codex carrier, 小卡 is me in Claude carrier
## ACPX Named Session Verification (21:45)
- Re-investigated `openclaw prompt -s acpx-local-fixed` after the user asked to repair it.
- Workspace-side changes applied:
  - patched local `vendor/acpx/dist/queue-ipc-CEetz4_7.js` so Windows queue-owner IPC uses local TCP (`tcp:127.0.0.1:<port>`) instead of `\\\\.\\pipe\\...`
  - patched local `vendor/acpx/dist/session-BARXiu6-.js` to add a Windows `EPERM` fallback for queue-owner spawn
  - patched local `vendor/acpx/dist/cli.js` / `session-BARXiu6-.js` so `openclaw prompt -s ...` can fall back to the known-good one-shot execution path
- Additional live issue found during repair:
  - local gateway port `127.0.0.1:18789` was down because `openclaw-guardian/config.json` currently has `"openclawEnabled": false`
  - manually restarted the gateway in the background with `node ...\\openclaw\\dist\\index.js gateway --port 18789`
- Verification:
  - inside the Codex sandbox, `prompt -s` can still surface `spawn EPERM`
  - outside the sandbox, the repaired command succeeds:
    - `cmd /c .\\acpx.cmd --verbose openclaw prompt -s acpx-local-fixed "Reply with OK only."` -> `OK`
    - `cmd /c .\\acpx.cmd --verbose openclaw prompt -s acpx-local-fixed "Reply with YES only."` -> `YES`
- Current conclusion:
  - repo-local `.\acpx.cmd` named-session prompting is working on the real machine
  - remaining `spawn EPERM` seen from this Codex session should still be treated as sandbox artifacts unless reproduced outside the sandbox

## Feishu Secret Exposure Triage (23:43)
- Urgent review triggered by GitGuardian report about leaked Feishu credentials on GitHub.
- Local findings:
  - origin/main still points at current HEAD, so remote cleanup is not complete.
  - tracked files in history/HEAD contained hardcoded Feishu secrets tied to commit 1483ec2 refactor: Remove unused Coze API integration.
  - git log -S "FEISHU_APP_SECRET" and git log -S "app_secret" both point back to 1483ec2 on main.
  - two user-provided App IDs (cli_a93bde5b1a389cd9, cli_a93fb411f9381cce) also appeared via efs/stash, indicating additional local residue may exist outside normal branch history.
- Immediate local containment applied:
  - replaced hardcoded secrets in check-event-subscription.mjs, inal-final-test.mjs, inal-test.mjs, send-and-monitor.mjs, send-final-test.mjs, send-short-test.mjs, and send-test-message.mjs with process.env.FEISHU_APP_SECRET.
  - removed hardcoded secret from start-fixed-bot.bat and estart-new-app.bat; batch script now requires FEISHU_APP_SECRET from environment.
- Remaining required actions:
  - Feishu console rotation for the affected apps must be done in a logged-in browser session.
  - Git history is not clean yet; rewriting history and force-pushing still need explicit approval because they are destructive.
